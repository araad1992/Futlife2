<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="com.ideamosweb.futlife.views.SelectGame">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TextView
            android:id="@+id/lbl_title"
            android:gravity="center"
            android:layout_marginTop="40dp"
            android:text="@string/select_game_title"
            android:textColor="@color/icons"
            android:textAllCaps="true"
            android:textSize="35sp"
            android:textStyle="bold"
            android:lines="1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
        <TextView
            android:id="@+id/lbl_subtitle"
            android:gravity="center"
            android:layout_marginTop="5dp"
            android:text="@string/select_game_subtitle"
            android:textColor="@color/icons"
            android:textAllCaps="true"
            android:textSize="22sp"
            android:textStyle="normal"
            android:lines="1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
        <include layout="@layout/content_select_game" />
    </LinearLayout>
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_next"
        android:layout_width="55dp"
        android:layout_height="55dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        app:backgroundTint="@color/colorAccent"
        app:fab_colorPressed="@color/colorAccentDark"
        app:fab_colorRipple="@color/colorAccentDark"
        app:elevation="6dp"
        android:src="@drawable/ic_next" />

    <!-- Bottom Sheet
    <include layout="@layout/bottom_sheet_consoles" /> -->

</android.support.design.widget.CoordinatorLayout>
